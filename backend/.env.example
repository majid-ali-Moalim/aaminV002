# PostgreSQL connection (required)
DATABASE_URL="postgresql://postgres:YOUR_PASSWORD@localhost:5432/aamin003"

# JWT (required)
JWT_SECRET="change-me-to-a-long-random-string"
JWT_REFRESH_SECRET="change-me-refresh-secret"
JWT_EXPIRES_IN="7d"

# Server
PORT=3002
FRONTEND_URL=http://localhost:3003

# Web Push — desktop notifications when browser tab is closed (optional)
# Generate keys: npx web-push generate-vapid-keys
WEB_PUSH_PUBLIC_KEY=
WEB_PUSH_PRIVATE_KEY=
WEB_PUSH_SUBJECT=mailto:support@aamin.so

# Email (optional — password reset)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password
SMTP_FROM=Aamin Ambulance <your-email@gmail.com>
